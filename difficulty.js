function GetDif(first, second){
    const hello = document.getElementById("lol").value
    hello+1
    first-1
    second+1
    if(hello < second+1 && hello > first-1){
        return parseInt(hello)
    }
    return 15
}